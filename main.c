
#include <stdio.h>
#include "common.h"
#include "math.h"
#include "printer.h"

void main()
{
	int a = 2;
	int b = 3;
	int c = 4;
	int res = 0;
	
	print_all(int a);
	res = addition(NUMBER, b);
	find_max(c, res);
}
